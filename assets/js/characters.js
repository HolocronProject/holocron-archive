document.addEventListener("DOMContentLoaded",async()=>{const t=document.getElementById("character-grid");if(!t)return;try{const r=await fetch("../assets/data/characters.json"),d=await r.json();t.innerHTML=d.map((c,i)=>`<article class="data-card"><div class="placeholder-mark">${String(i+1).padStart(2,"0")}</div><h3>${c.name}</h3><p>${c.role}<br>${c.era}</p><span class="badge">準備中</span></article>`).join("")}catch(e){t.innerHTML='<p class="notice">キャラクターデータを読み込めません。</p>'}});