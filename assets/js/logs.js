document.addEventListener("DOMContentLoaded",async()=>{const r=rootPath(),d=await loadJSON(`${r}/assets/data/${document.body.dataset.logFile}`),l=document.querySelector("#log-list");l.innerHTML=d.sort((a,b)=>b.date.localeCompare(a.date)).map(x=>`<article class="log-entry"><time>${x.date}</time><h2>${x.title}</h2><p>${x.body}</p><div class="tags">${(x.tags||x.updates||[]).map(t=>`<span class="tag"># ${t}</span>`).join("")}</div></article>`).join("")});