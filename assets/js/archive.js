(async()=>{const grid=document.querySelector('#archive-grid');let rows=[];function render(mode='all'){grid.innerHTML=rows.filter(x=>mode==='all'||x.media.includes(mode)).map(x=>`<article class="character-card"><div class="pixel-avatar">${x.icon}</div><div class="card-copy"><span class="badge">${x.media[0].toUpperCase()}</span><h2>${x.name}</h2><p class="english">${x.en}</p><dl><dt>時代</dt><dd>${x.era}</dd><dt>所属</dt><dd>${x.affiliation}</dd>${x.master?`<dt>師匠</dt><dd>${x.master}</dd>`:''}</dl><p>${x.summary}</p><div>${x.works.map(w=>`<span class="work-tag">${w}</span>`).join('')}</div></div></article>`).join('')}try{rows=await fetch('../assets/data/characters.json').then(r=>r.json());render();document.querySelector('.filter-row').addEventListener('click',e=>{const b=e.target.closest('.chip');if(!b)return;document.querySelectorAll('.chip').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.filter)})}catch(e){grid.innerHTML='<p>データを読み込めませんでした。</p>'}})();
